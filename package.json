{
  "name": "iron-circuit",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "A browser fighting game in one self-contained HTML file.",
  "scripts": {
    "test": "node tests/behaviour.mjs && node tests/golden.mjs",
    "test:behaviour": "node tests/behaviour.mjs",
    "test:golden": "node tests/golden.mjs",
    "test:record": "node tests/golden.mjs --update",
    "build": "node tools/build.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
