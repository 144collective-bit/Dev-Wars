{
  "name": "iron-circuit",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "A browser fighting game in one self-contained HTML file.",
  "scripts": {
    "test": "node tools/build.mjs --check && node tests/behaviour.mjs && node tests/golden.mjs",
    "test:behaviour": "node tests/behaviour.mjs",
    "test:golden": "node tests/golden.mjs",
    "test:record": "node tests/golden.mjs --update",
    "build": "node tools/build.mjs",
    "build:check": "node tools/build.mjs --check"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
